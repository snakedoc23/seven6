<!DOCTYPE html>
<html lang="pl">
<head>
  <title>Seven6</title>
	<meta charset="UTF-8">
  <%= stylesheet_link_tag "style" %>
  <%= javascript_include_tag :defaults, "http://www.google.com/jsapi", "http://maps.googleapis.com/maps/api/js?sensor=false&libraries=geometry", "jquery-ui-1.8.16.custom.min", "infobox", "markerclusterer_packed", "rating", "comment", "edit_user", "route_ajax", "stats", "highcharts", "climbs", "workouts" %>
  <%= csrf_meta_tag %>
</head>
<body>
	<div id="ajax-status"></div><!-- /ajax-status -->
	<%= render 'layouts/header' %>
	<div id="container">
		<% flash.each do |key, value|%>
		<div class="flash <%= key %>">
			<%= value %>
		</div>
		<% end %>
		<%= yield %>
		<div class="clear"></div>
		
	</div>
	<%= render 'layouts/footer' %>
	<%# debug(params) if Rails.env.development? %>
</body>
</html>
